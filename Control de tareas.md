Login/register => {email, password}             CECILIA
               <= {token}
Crear partida  => { settings }
Sala de espera => Wait for websockets?          ROGELIO
Partida        => websockets 
Fin de partida => websockets
               <= {infoPartida} 
Historial de partidas => {playerID}             CARLOS
                      <= {historialPartidas}
navbar¿?             => {gameId}
                     <= {websockets}